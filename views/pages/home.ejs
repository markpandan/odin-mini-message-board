<ul class="messages-list">
  <% messages.forEach((value) => { %>
  <li class="message-item" onclick= <%= `window.location.href='/message/${value.id}'` %> >
    <h1><%= value.username %></h1>
    <h5><%= value.added %></h5>
    <p><%= value.message %></p>
  </li>
  <% }) %>
</ul>
