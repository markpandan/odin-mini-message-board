<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <%- include('navbar') %> <% if (page == 'home') { %> <%-
    include('pages/home', {messages: messages}) %> <% } else if (page == 'new')
    { %> <%- include('pages/new', {messages: messages}) %> <% } %>

    <button class="add-message-btn" onclick="window.location.href = '/new'">
      +
    </button>
  </body>
</html>
